変数numはプリミティブ型で、変数array[0]は参照型であるため。

プリミティブ型の引数をメソッドに渡す際は、値渡しになる。
値渡しは、メソッド内でnumの値を変更しても、呼び出し元の変数numには影響がない。(ローカルスコープ内でのみ有効)

参照型の引数を渡す際は、参照渡しです。
参照渡しは、配列の参照が渡され、メソッド内でその参照を介して配列の要素を変更すると、呼び出し元の配列も変更される。
